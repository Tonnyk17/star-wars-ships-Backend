type Mutation {
  createFilm(title: String!): Film
}

type Query {
  ping: String
  films: [Film!]
}

type Film {
  _id: ID!
  title: String!
}

type Ship {
  _id: ID!
  name: String
  model: String
  starshipClass: String
  manufacturers: [String]
  costInCredits: Float
  length: Float
}
